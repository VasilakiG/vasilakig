name: Update Fun Fact

on:
  schedule:
    - cron: '0 0 * * *'  # Run the job every day at midnight (UTC)

jobs:
  update-fun-fact:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Update Fun Fact
        run: |
          API_KEY="${{ secrets.X_API_KEY }}"
          FUN_FACT=$(curl -s -H "X-Api-Key: $API_KEY" "https://api.api-ninjas.com/v1/facts?limit=1" | jq -r '.[0].fact')
          if [[ "$FUN_FACT" == "null" ]]; then
            FUN_FACT="Failed to fetch fun fact."
          fi
          sed -i "s|⚡ Fun fact:.*|⚡ Fun fact: **$FUN_FACT**|" README.md
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git commit -m "Update fun fact" -a
          git push
